# 产品形态

更新时间：2026-08-25

这不是实现计划。它修正「打开软件 = 拿走一份执行摘要」的默认想象。身份与视觉仍见 `PRODUCT.md` / `DESIGN.md`；环怎么跑见 `AINative.md`。

---

## 结论

工作流固定的是**决策树**，不是一种产物。

同一个人、同一条链接，也会先要能引用的摊开稿，再要一张短表。场景是这一次要干什么，不是两种用户二选一。把「扫一眼 / 坐下来」写成产品分叉，是错的。

市面上已经有音频转录，也会压成要点。MediaBrief 若打开永远是「大意 + 3–7 条」，那只是多付一次 token 的同质功能。别人用这软件，是因为从链接到可做事的文字这条链它自己跑完，而且跑完之后还对得起这场内容。

AI-native 不是让模型把摘要写得更圆。是现场该按哪颗按钮就按哪颗：有字幕别转音频，循环就切片，专名吃不准就问人，访谈就按说话顺序摊开，积压就出短表，RSS 就推能扫完的那张。规则够用宿主自己做，不够用才进环。这才是少浪费 token。

`PRODUCT.md` 里「贴链接、读完摘要、拿走 Markdown、别想界面」只覆盖积压扫一眼。它不能再当唯一成功标准。

---

## 现在锁死在哪

抽取可以分叉（字幕 / Whisper / 媒体恢复）。稿出来之后不能：

- 优化是一次改写，失败就退回原文
- 摘要角色写死执行摘要：约 180–450 词、3–7 条 takeaways，禁止摊开
- 摘要先出，且吃的是没校对的 raw
- 页面只读：复制、导出、重跑、再生成摘要。不能改专名，不能标幻觉，不能再问一句
- Harness 只在「字幕和音频都失败」时转。稿差也算 `completed`

结果：拿不到文件有环；文件有了但字是错的、该摊开还是该压短，环都看不见。校对被降成没有工具的一次改写。

---

## 该固定的工作流

真实转写不是「录音 → 逐字稿」一条直线。

```text
拿到素材
  ├─ 本地录音（m4a 等）
  │     ffmpeg → 16kHz 单声道 → Whisper turbo（不要 small 出成品）
  │     先看最短一段有没有循环幻觉
  │
  └─ 视频链接
        yt-dlp 先探字幕
        ├─ 有可用字幕 → 只下字幕，不转音频
        └─ 没有       → 只下音频 → Whisper turbo
              （专名应进识别，不要只靠事后改写）

无论哪条路，后面都一样，而且不是可选装饰：
  专名校正（平台字幕也会把马寅听成马云）
  按停顿分段（太粗要重切）
  片头 / 广告 / 旁白切开
  发现循环幻觉 → 切片重转，不要整集重来
  机器稿是底稿；人改专名才是逐字稿
```

小模型只探路，大模型出底稿。默认 `large-v3-turbo` 已经对；缺的是稿不好时要把环用起来，而不是再写一条没有 Tool 的 `chat.completions`。

宿主已经能做、且不该再问模型的：16k 单声道、字幕优先、turbo 默认、固定步进循环过滤、最多切一段重转。环该接的是宿主验不了真值的那些：专名、片头 BGM、旁白广告、这一场该摊开还是该压短。

---

## 产物按场景选，不按人群选

同一场访谈可以先后是两份东西，顺序不能反：

1. **简报**：按谈话顺序摊开双方观点、分歧、例子、数字。目标是不忽略有价值的信息。
2. **短表**：执行摘要、关键要点。目标是积压时扫完能走。压缩是后一步，默认不是压缩。

Cursor 对同一条 Lenny × Altman 产出的是前者；MediaBrief 当前产出的是后者（还带上 `Toby Lu` 这种专名错误）。喜欢前者，不是因为文笔，是因为体裁不同。

RSS / Bot 可以默认短表。桌面打开一场长访谈不应只有短表。用户要另一份时，应能从同一底稿再做，而不是重跑整条管线。

---

## Harness 用在稿上，不是用在「再写圆一点」

加场景的旧门槛是「成功条件必须宿主能验证」。媒体恢复的成功是有校验过的字幕或音频。错别字、阿那亚 / 马寅、这一场该不该摊开，没有宿主真值。按旧门槛，校对进不了环——所以才闲置。

这不等于去造聊天页、通用 Agent 或旁路 `chat.completions`。还是同一套：场景、可见 Tool 表、同一 `execute`、问人用固定按钮。缺的是稿这一侧的按钮，例如对着可疑区间再转、咬住标题里的专名、把片头/广告从对谈里剥开、从底稿生成简报或短表。

模型不会比宿主更会做事。它只是在稿不好、或这一场不该压成要点时，按下已经存在的按钮。
