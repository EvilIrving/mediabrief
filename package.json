{
  "name": "ai-transcriber",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"cd backend && uvicorn main:app --host 127.0.0.1 --port 8000 --reload\" \"cd frontend && pnpm dev -- --host 127.0.0.1\"",
    "dev:api": "cd backend && uvicorn main:app --host 127.0.0.1 --port 8000 --reload",
    "dev:web": "cd frontend && pnpm dev -- --host 127.0.0.1",
    "build": "cd frontend && pnpm install && pnpm build",
    "install:all": "cd frontend && pnpm install",
    "lint": "cd frontend && pnpm lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
