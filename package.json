{
  "name": "mediabrief",
  "private": true,
  "license": "MIT",
  "scripts": {
    "stop": "lsof -ti :8000 -ti :5173 | xargs kill -9 2>/dev/null || true; pkill -f \"$PWD/venv/bin/uvicorn main:app\" 2>/dev/null || true; pkill -f \"mediabrief.*start.py\" 2>/dev/null || true; ps -axo pid=,command= | awk '/[P]ython .*start.py/ {print $1}' | xargs kill -9 2>/dev/null || true; echo stopped",
    "serve:api": "cd backend && ../venv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8000",
    "dev:api": "pnpm stop; pnpm serve:api",
    "dev:web": "cd frontend && pnpm exec vite --host 127.0.0.1",
    "dev": "pnpm stop; concurrently -k -n api,web -c blue,green \"pnpm serve:api\" \"pnpm dev:web\"",
    "dev:chrome": "pnpm stop; (sleep 2.5 && open -na \"Google Chrome\" --args --app=http://127.0.0.1:5173) & concurrently -k -n api,web -c blue,green \"pnpm serve:api\" \"pnpm dev:web\"",
    "dev:safari": "pnpm stop; (sleep 2.5 && open -na \"Safari\" \"http://127.0.0.1:5173\") & concurrently -k -n api,web -c blue,green \"pnpm serve:api\" \"pnpm dev:web\"",
    "dev:window": "pnpm dev:chrome",
    "build": "cd frontend && pnpm install && pnpm build",
    "install:all": "cd frontend && pnpm install",
    "lint": "cd frontend && pnpm lint",
    "test:api": "cd backend && ../venv/bin/python -m pytest",
    "test:web": "cd frontend && pnpm test",
    "test": "pnpm test:api && pnpm test:web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
