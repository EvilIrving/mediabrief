# MediaBrief macOS 极简产品化计划

更新时间：2026-08-13

定位：P0 产品化的代码与基线已完成——Developer ID 签名/公证/Gatekeeper 基线、启动可靠性、默认模型后台准备、官方源失败换国内镜像、Agent 可见的运行环境画像、发行配置隐藏设置、下载页/隐私页/应用内检查更新、错误分类文案均已落地。本文件只保留尚未完成的事项，已完成的记录已清理，避免与 AI Native 架构（`AINative.md`）交叉冲突。

目标平台：Apple Silicon Mac，Developer ID 直接分发，不上 Mac App Store

## 产品边界

- 只做一个发行版本；发行版内置可用 API 配置，费用由产品作者承担，真实 Key 不提交 Git。
- 接受发行配置可能被提取的风险；成本通过上游供应商控制台观察，异常时停用或轮换 Key。
- 视频下载、Cookie、FFmpeg、Whisper、历史数据库和媒体文件继续在本地处理。
- 任务超时、取消和本地并发控制只服务于应用稳定性，不扩展成商业风控。
- MediaBrief 是 macOS 转录软件：只在 macOS（Apple Silicon）上发行。

## 现在明确不做

- 社区版 / 商业版双发行 Profile；BYOK 或让普通用户选择 API 供应商；Hosted AI 服务和通用 API 中转。
- 注册、登录、账户系统；激活码、邀请码、试用、兑换；设备绑定、设备会话和授权状态；套餐、额度、正文限额、频率限制、速率限制；支付、订阅和商业后台；为未来收费预留接口、数据库或权限结构。
- 静默差分自动更新；全量遥测、用户画像或复杂分析平台；重做整个 UI；为发布流程搭建通用测试框架。
- 微型 coding agent、动态 extractor 或 Agent Harness —— 注：应用内恢复 Harness（见 `AINative.md`）是已批准的架构演进，不在此拒绝范围内；此处拒绝的是编程 Harness 与通用平台。

未来真的开始收费，或真的发生持续滥用时，再根据已经出现的问题增加对应能力，不提前设计。

## Beta 完成标准（剩余待办的验收依据）

同时满足以下条件，才称为可分发 Beta：

1. 产出带版本号、完成 Developer ID 签名与 Apple 公证的 Apple Silicon DMG。
2. 在没有源码和开发环境的干净 macOS 用户环境中可以安装、启动。
3. 用户不打开终端、不配置 API，能够完成一个公开链接任务和一个本地媒体任务。
4. 启动或任务失败时有明确提示、重试方式和日志入口，不会无限停在“正在初始化”。
5. 应用重启后历史记录和设置仍在。
6. 用户可以看到版本、打开日志目录、复制诊断和找到新版下载入口。
7. 至少 5 位非技术测试者只拿到 DMG 和一句产品说明，也能独立完成第一次任务。

## 剩余待办

已完成的代码与基线（签名公证、启动可靠性、模型准备、换源、环境画像、隐藏设置、下载页/隐私页/检查更新、错误分类文案）不再列入。剩余事项：

1. **真实发行配置验收**：带真实发行 AI 配置再跑一遍 `scripts/release_macos.sh`，产出正式 DMG；顺带验证失败分类文案在真实配置下的实际效果（后端 `humanize_error_code` 与前端四语言 `error.*` 文案已就位，未在真实发行环境验证）。
2. **干净 Mac 验收**：在没有源码、Python、Node、Homebrew 和开发者证书的 Apple Silicon Mac 上走完整安装流程，真实下载的 DMG 通过 Gatekeeper 并完成第一次任务。
3. **场景测试矩阵**：公开 YouTube/Bilibili 链接、本地文件、无字幕任务、无网络、端口冲突、运行中退出、历史恢复、覆盖安装升级。
4. **五人非技术验收**：至少 5 位非技术用户，只给 DMG 和一句产品说明，记录成功与否以及卡住的位置。
5. **GitHub Pages 运维**：仓库设置把 Source 指到 `docs/`，下载页才会出现在 https://evilirving.github.io/mediabrief/ 。

已决定不做：静默应用更新；降级转录在结果上打标；关窗时提示任务将被停止的文案（保持通用确认）。

## 执行纪律

- 开始前阅读根目录 `AGENTS.md` 和本文件，并检查工作区已有改动。
- 一次只处理当前任务，不顺手建设未来商业化能力。
- 不提交真实 API Key、Apple 凭据、Cookie 或用户内容。
- 签名、公证、staple 和 Gatekeeper 验证必须失败关闭。
- 未经用户明确要求，不提交、不推送、不上传发行物。
- 完成任务后，只记录实际改动、验证结果和仍存在的问题。

## 下一项代码任务

> 只处理带真实发行配置的干净 Mac 验收。不要做静默应用更新，也不要给降级转录打标；不要重做端口、单实例、模型等待、换源或恢复 Loop（恢复 Loop 的演进归 `AINative.md` / `AINativePlan.md`）。
