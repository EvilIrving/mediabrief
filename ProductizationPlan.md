# MediaBrief macOS 极简产品化计划

更新时间：2026-08-13

优先级：**P0，当前最高优先级**

当前状态：签名、公证与 Gatekeeper 基线已完成；启动可靠性、默认模型后台准备、发行配置隐藏设置、官方源失败自动换国内镜像，以及 Agent 可见的运行环境画像均已落地。下一件是 Task 3/4 的剩余产品化缺口，不要回头重做已完成项。

目标平台：Apple Silicon Mac，Developer ID 直接分发，不上 Mac App Store

## 目标

只解决一件事：把 MediaBrief 从“开发者能运行的项目”变成“普通用户下载 DMG、拖入 Applications、双击打开后就能完成第一次任务的产品”。

最终路径：

```text
下载 DMG
  → 拖入 Applications
  → 双击打开
  → 粘贴链接或拖入文件
  → 自动下载、转录、总结
  → 查看、复制或导出结果
  → 再次打开时历史仍在
```

普通用户不需要：

- 打开终端；
- 安装 Python、Node.js、Homebrew、FFmpeg 或 Deno；
- 启动后台服务；
- 填写 API Key、Base URL 或模型名；
- 理解内部技术错误；
- 决定是否下载模型、选择模型版本或手动处理约 1.6 GB 的模型文件。

## 首次启动的产品契约

- 应用主窗口必须尽快出现，同时立即在后台自动准备完整运行环境。
- `large-v3-turbo` 是正常使用路径：首次启动即自动下载，支持中断恢复、失败重试和网络恢复后继续，不要求用户确认、选择模型或进入设置。
- 下载期间允许不依赖 Whisper 的字幕任务先完成；确实需要本地转录的任务等待默认模型就绪，不得因为模型仍在下载而静默使用 `base`。
- `base` 只是在默认模型因断网、持续下载失败或设备环境异常而无法使用时的应急兜底。发生兜底必须明确告知用户当前处于降级模式及质量影响，不能把它伪装成正常完成路径。
- FFmpeg、FFprobe、Deno、MLX、yt-dlp 等组件由发行版自动提供、检查和维护；用户不参与安装、版本选择或故障排查。
- 初始化过程必须有统一、可理解的状态；网络切换或暂时离线不能让初始化永久卡住，恢复网络后应自动继续。

## 已确定的产品边界

- 只做一个发行版本，不区分社区版、商业版或官方版。
- 发行版内置可用的 API 配置，API 费用由产品作者承担。
- API Key 和模型配置可在构建发行版时注入，但不把真实 Key 提交进 Git 仓库。
- 接受发行版中的配置可能被提取这一风险。成本直接通过上游供应商控制台观察；发现异常时停用或轮换 Key。
- 现在不建设服务端代理、secret store、用户账户、激活授权或反滥用系统。
- 现有任务超时、取消和本地并发控制只服务于应用稳定性，不扩展成商业风控。
- 视频下载、Cookie、FFmpeg、Whisper、历史数据库和媒体文件继续在本地处理。
- 第一版只支持 Apple Silicon，不承诺 Intel Mac、Windows 或 Mac App Store。

## 现在明确不做

- 社区版 / 商业版双发行 Profile；
- BYOK 或让普通用户选择 API 供应商；
- Hosted AI 服务和通用 API 中转；
- 注册、登录、账户系统；
- 激活码、邀请码、试用、兑换；
- 设备绑定、设备会话和授权状态；
- 套餐、额度、正文限额、频率限制和速率限制；
- 支付、订阅和商业后台；
- 为未来收费预留接口、数据库或权限结构；
- 静默差分自动更新；
- 全量遥测、用户画像或复杂分析平台；
- 重做整个 UI；
- 微型 coding agent、动态 extractor 或 Agent Harness；
- 为发布流程搭建通用测试框架。

未来真的开始收费，或真的发生持续滥用时，再根据已经出现的问题增加对应能力，不提前设计。

## Beta 完成标准

同时满足以下条件，才称为可分发 Beta：

1. 产出带版本号、完成 Developer ID 签名与 Apple 公证的 Apple Silicon DMG。
2. 在没有源码和开发环境的干净 macOS 用户环境中可以安装、启动。
3. 用户不打开终端、不配置 API，能够完成一个公开链接任务和一个本地媒体任务。
4. 启动或任务失败时有明确提示、重试方式和日志入口，不会无限停在“正在初始化”。
5. 应用重启后历史记录和设置仍在。
6. 用户可以看到版本、打开日志目录、复制诊断和找到新版下载入口。
7. 至少 5 位非技术测试者只拿到 DMG 和一句产品说明，也能独立完成第一次任务。

## 当前实现与缺口

已经具备，不要再当缺口重做：

- `start.py` 用 pywebview 开窗口；监听 `127.0.0.1` 系统分配端口，不再依赖 8000 空闲。
- 同一数据目录有单实例锁，连续双击不会起两个后端。
- 主窗口先出加载页；服务失败可重试或打开日志目录。日志写在 Application Support，不写 `.app` bundle。
- 关闭窗口会清理任务子进程；`confirm_close=True` 已开，但文案还不是“正在运行的任务会被停止”。
- FFmpeg、FFprobe、Deno、MLX 和 Whisper `base` 已有随包方案；启动时查找并写入诊断。
- `large-v3-turbo` 首启后台下载，带进度、退避重试和续传。需要本地转录的任务会等待；`_resolve_available_size()` 不再静默改成 `base`。连续失败后才明确降级，界面有降级提示。
- 默认模型先走官方 Hugging Face，失败后立刻换 `https://hf-mirror.com`；状态记录当前源和已尝试的源。
- yt-dlp 有随包基线、可写副本、原子替换、每周检查和 `update_status()`。
- `/api/diagnostics`、`/api/environment-status`、`/api/model-status` 已能读组件和模型状态；恢复 Agent 的 `inspect_runtime` 读同一份运行时画像。
- 根目录 `VERSION` 驱动应用版本和发行文件名；`scripts/release_macos.sh` 串联构建、签名、公证、DMG。
- 发行配置存在时，前端隐藏 API Key、Base URL、模型选择和 Whisper 选择。
- Developer ID 签名、公证、staple、Gatekeeper 基线已跑通。
- 单页下载/隐私说明在 `docs/index.html`。应用内「检查更新」只打开该页，没有静默更新。降级转录不在结果上打标。

仍需解决：

- 关窗确认还不会说明任务会被停止。
- 正式发行还需带真实发行 AI 配置再跑一遍 `release_macos.sh`，并在干净 Mac 上验收。
- 还没有五人非技术验收。
- GitHub Pages 需要在仓库设置里把 Source 指到 `docs/`，下载页才会出现在 https://evilirving.github.io/mediabrief/ 。

---

## Task 1 — 完成可信发行物（代码与基线已完成）

状态：`[x] 代码与公证基线已完成；干净 Mac + 真实发行配置的完整验收仍待 Task 4`

### 要做

- 完成 Developer ID 签名、公证、staple 和 Gatekeeper 验证，任何一步失败都不能报告发行成功。
- 使用 `notarytool`，不再使用已经停止接受上传的 `altool`。
- 用一个版本值驱动应用版本和 DMG 文件名。
- 一条明确命令从当前源码生成版本化 DMG。
- DMG 中只包含可以直接安装的 `MediaBrief.app` 和 Applications 快捷方式。

### 当前进展

2026-08-13：

- 已重写 `scripts/sign_and_package.sh`，签名、公证、staple、DMG 内容和 Gatekeeper 检查均改为失败即停止。
- 已完成真实 Developer ID 签名、secure timestamp、嵌套代码验证、Deno 运行验证和签名后应用启动验证。
- 已复用用户现有的 App Store Connect 团队 API Key 与 Issuer ID，并将验证后的凭据保存到 `mediabrief-notary` Keychain profile；除 `notarytool` 验证与保存凭据外，未人工查看、打印或复制 `.p8` 私钥内容。
- app 与 DMG 两次 Apple 公证均返回 `Accepted`，二者 staple/validate 成功；最终 DMG 挂载后，内含 app 与 DMG 本身均被 Gatekeeper 接受为 `Notarized Developer ID`。
- 已生成正式基线产物 `dist/MediaBrief-1.0.0-macos-arm64.dmg` 及其发行清单 `dist/MediaBrief-1.0.0-macos-arm64-manifest.json`。
- 根目录 `VERSION` 已作为应用、构建包和诊断的单一版本来源接入，等待下一次正式构建验证。

### 完成标准

- 正式 DMG 完成签名、公证和 staple。
- 从 DMG 拖入 Applications 后可以正常打开。
- 应用版本与 DMG 文件名一致。

---

## Task 2 — 让双击启动与运行环境可靠

状态：`[x] 主体已完成`

签名基线推进期间，启动可靠性已经在代码里落地。下面“要做”里的条目是当时的清单；括号里是 2026-08-13 对照代码后的结果。

### 要做

- 自动选择可用的本地端口，不依赖 8000 空闲。**已完成**（`start.py` 绑定端口 0）。
- 避免同时启动多个后端实例争抢数据和端口。**已完成**（`single_instance.py`）。
- 主窗口尽早出现；重型模型和依赖加载不能让窗口长时间无响应。**已完成**。
- 主窗口出现后立即自动准备 `large-v3-turbo`，向前端提供下载进度、当前状态和失败原因；支持断点续传、重试以及网络恢复后继续。**已完成**（官方源失败后自动换国内镜像，见 Plan Task 7）。
- 需要本地转录的任务在默认模型下载期间进入明确的“正在准备转录环境”状态，不得静默改用 `base`；只有默认模型确认无法准备时才能进入有提示的降级路径。**已完成**（降级结果尚未在成品上打标）。
- 启动时检查 FFmpeg、FFprobe、Deno、MLX 和 yt-dlp 的可用性与版本，把结果纳入统一诊断；内置组件缺失或不可执行时明确失败，不能等到任务中途才暴露。**诊断已有**；缺的是 Agent 能读同一份画像，以及缺失组件时的友好说明。
- yt-dlp 保留随包可用基线版本，并在可写目录中自动更新；更新必须原子替换、失败回退，能够报告当前版本、随包版本、上次检查和上次更新结果。**已完成**。
- 启动失败时显示重试、复制诊断、打开日志目录和退出。**加载页已有重试和打开日志**。
- 所有启动错误统一写入 Application Support，不写 `.app` bundle。**已完成**。
- 有任务运行时关闭窗口，要明确提示任务将被停止；确认后清理子进程。**清理已完成**；关窗文案仍是通用确认。

### 完成标准

- 8000 被占用时仍能启动。**已满足**
- 后端启动失败时不会永久转圈。**已满足**
- 连续双击不会产生两个同时工作的 MediaBrief 实例。**已满足**
- 用户可以自行找到日志并重试。**已满足**
- 首次启动会自动开始准备默认大模型，下载中断或切换网络后可以继续，不要求用户进入设置。**已满足**
- 默认模型准备期间不会有任务在用户不知情时使用 `base` 完成转录。**已满足**
- 诊断信息可以确认 FFmpeg、FFprobe、Deno、MLX、yt-dlp 和 Whisper 默认模型是否就绪及其版本/状态。**API 已满足**
- 退出后没有残留 FFmpeg、yt-dlp 或 uvicorn 进程。**清理路径已满足**

### 未做完、转到 Task 3/4

- 关窗说明正在运行的任务会被停止。
- 降级转录在结果上标明应急质量。

---

## Task 3 — 装完即可完成第一次任务

状态：`[-] 部分完成`

前置：Task 2 主体已完成；换源和环境可见性在 Plan.md Task 7。发行配置隐藏设置已经落地，本任务剩下真实发行配置验收和失败分类文案。

### 要做

- 移除首次使用中的 API Key、Base URL、获取模型和模型供应商配置。
- 发行版直接使用内置 API 配置；不增加登录、激活、兑换或授权步骤。
- 首屏直接提供粘贴链接和拖入本地文件。
- 任务进度只显示用户能理解的阶段：读取视频、查找字幕、下载音频、转录、生成摘要。
- 成功后默认显示摘要，同时可以查看全文、复制和导出。
- 正常转录统一使用首启自动准备好的 `large-v3-turbo`；不向普通用户暴露模型下载、模型选择或供应商配置。
- 保留内置 `base` 作为最后应急能力，但只在默认模型确认无法使用时显式进入降级模式。
- 失败时区分链接不可用、本地组件问题、模型未准备和 AI 请求失败，并给出下一步。
- 重启后恢复历史记录和设置。

### 完成标准

- 新用户不进入设置就能提交任务。
- 带字幕链接可在默认模型后台准备期间直接完成，不被模型下载阻塞。
- 本地文件和无字幕链接默认使用 `large-v3-turbo`；模型尚在准备时显示等待状态，不能静默使用 `base`。
- 默认模型持续无法准备时，应用明确说明降级原因后才可使用内置 `base` 完成任务。
- AI 请求失败时保留已完成的下载和转录，允许重试摘要。
- 四种 UI 语言没有新增硬编码文案。

---

## Task 4 — 发布与真实用户验收

状态：`[-] 下载页与检查更新已完成；干净 Mac 与五人验收未做`

前置：Task 1–3 主体已完成。

### 要做

- 提供一个简单下载页，包含最新版本、系统要求、DMG 入口和已知限制。**已完成**（`docs/index.html`；DMG 走 GitHub Releases，未把未发布本地包的 SHA 写上公开页）。
- 应用内显示当前版本，并提供“检查更新”；第一版只打开下载页，不做静默更新。**已完成**。
- 提供最小隐私说明：哪些内容在本地处理、哪些文本会发给上游 AI、Cookie 不会发送、数据如何删除。**已完成**（同一页 `#privacy`）。
- 提供打开日志目录、复制诊断和反馈入口；诊断不自动上传。**部分完成**（设置里展示数据目录；加载失败页可打开日志）。
- 在没有源码、Python、Node、Homebrew 和开发者证书的 Apple Silicon Mac 上走完整安装流程。
- 测试公开 YouTube/Bilibili 链接、本地文件、无字幕任务、无网络、端口冲突、运行中退出、历史恢复和覆盖安装升级。
- 找至少 5 位非技术用户，只给 DMG 和一句产品说明，记录是否成功以及卡住的位置。

### 完成标准

- 真实下载后的 DMG 能通过 Gatekeeper 并完成第一次任务。
- 用户遇到失败时知道下一步，不需要开发者远程修环境。
- 覆盖安装新版本后历史和设置仍在。
- 下载页、隐私说明、反馈入口和发行说明可用。

## 执行顺序

```text
Task 1 可信发行物（代码与公证基线已完成）
Task 2 双击启动与运行环境可靠性（主体已完成）
Plan.md Task 7 宿主换源 + Agent 看见环境（已完成）
  → Task 3 第一次任务剩余验收
  → Task 4 干净 Mac 与真实用户 Beta
```

不要回头重做端口、单实例、模型等待或签名脚本。Task 3 不依赖账号、授权或 hosted 服务。

## 执行纪律

- 开始前阅读根目录 `AGENTS.md` 和本文件，并检查工作区已有改动。
- 一次只处理当前任务，不顺手建设未来商业化能力。
- 不提交真实 API Key、Apple 凭据、Cookie 或用户内容。
- 签名、公证、staple 和 Gatekeeper 验证必须失败关闭。
- 未经用户明确要求，不提交、不推送、不上传发行物。
- 完成任务后，只记录实际改动、验证结果和仍存在的问题。

## 下一项代码任务

> 下载页、隐私说明和「检查更新打开该页」已完成。不要做静默应用更新，也不要给降级转录打标。下一件若做代码，只处理关窗时说明任务会被停止，或带真实发行配置的干净 Mac 验收。不要重做端口、单实例、模型等待、换源或恢复 Loop。
