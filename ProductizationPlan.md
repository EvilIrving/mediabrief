# MediaBrief macOS 极简产品化计划

更新时间：2026-08-13

优先级：**P0，当前最高优先级**

当前状态：签名与公证工作进行中；其余产品化工作尚未开始

目标平台：Apple Silicon Mac，Developer ID 直接分发，不上 Mac App Store

## 目标

只解决一件事：把 MediaBrief 从“开发者能运行的项目”变成“普通用户下载 DMG、拖入 Applications、双击打开后就能完成第一次任务的产品”。

最终路径：

```text
下载 DMG
  → 拖入 Applications
  → 双击打开
  → 粘贴链接或拖入文件
  → 自动下载、转录、总结
  → 查看、复制或导出结果
  → 再次打开时历史仍在
```

普通用户不需要：

- 打开终端；
- 安装 Python、Node.js、Homebrew、FFmpeg 或 Deno；
- 启动后台服务；
- 填写 API Key、Base URL 或模型名；
- 理解内部技术错误；
- 在不知情时下载约 1.6 GB 模型。

## 已确定的产品边界

- 只做一个发行版本，不区分社区版、商业版或官方版。
- 发行版内置可用的 API 配置，API 费用由产品作者承担。
- API Key 和模型配置可在构建发行版时注入，但不把真实 Key 提交进 Git 仓库。
- 接受发行版中的配置可能被提取这一风险。成本直接通过上游供应商控制台观察；发现异常时停用或轮换 Key。
- 现在不建设服务端代理、secret store、用户账户、激活授权或反滥用系统。
- 现有任务超时、取消和本地并发控制只服务于应用稳定性，不扩展成商业风控。
- 视频下载、Cookie、FFmpeg、Whisper、历史数据库和媒体文件继续在本地处理。
- 第一版只支持 Apple Silicon，不承诺 Intel Mac、Windows 或 Mac App Store。

## 现在明确不做

- 社区版 / 商业版双发行 Profile；
- BYOK 或让普通用户选择 API 供应商；
- Hosted AI 服务和通用 API 中转；
- 注册、登录、账户系统；
- 激活码、邀请码、试用、兑换；
- 设备绑定、设备会话和授权状态；
- 套餐、额度、正文限额、频率限制和速率限制；
- 支付、订阅和商业后台；
- 为未来收费预留接口、数据库或权限结构；
- 静默差分自动更新；
- 全量遥测、用户画像或复杂分析平台；
- 重做整个 UI；
- 微型 coding agent、动态 extractor 或 Agent Harness；
- 为发布流程搭建通用测试框架。

未来真的开始收费，或真的发生持续滥用时，再根据已经出现的问题增加对应能力，不提前设计。

## Beta 完成标准

同时满足以下条件，才称为可分发 Beta：

1. 产出带版本号、完成 Developer ID 签名与 Apple 公证的 Apple Silicon DMG。
2. 在没有源码和开发环境的干净 macOS 用户环境中可以安装、启动。
3. 用户不打开终端、不配置 API，能够完成一个公开链接任务和一个本地媒体任务。
4. 启动或任务失败时有明确提示、重试方式和日志入口，不会无限停在“正在初始化”。
5. 应用重启后历史记录和设置仍在。
6. 用户可以看到版本、打开日志目录、复制诊断和找到新版下载入口。
7. 至少 5 位非技术测试者只拿到 DMG 和一句产品说明，也能独立完成第一次任务。

## 当前实现与缺口

已经具备：

- `start.py` 能启动本地 uvicorn，并用 pywebview 打开桌面窗口。
- 已有 PyInstaller spec、macOS 构建脚本和签名打包脚本。
- FFmpeg、FFprobe、Deno、MLX 和 Whisper base 模型已有随包方案。
- 日志和运行数据大部分写入 `~/Library/Application Support/ai-transcriber/`。
- 当前只支持 Apple Silicon，与本轮目标一致。

仍需解决：

- 完成正式公证、staple 和 Gatekeeper 验证。
- 建立一个版本来源，供应用信息和 DMG 文件名共同使用。
- `start.py` 固定使用 `127.0.0.1:8000`，端口冲突时无法正常启动。
- 部分启动错误可能写入 `.app/Contents/MacOS`，应统一写入 Application Support 日志目录。
- 初始化失败时缺少明确的失败、重试和日志入口。
- 启动时会预加载重型依赖，并静默触发 `large-v3-turbo` 下载。
- 当前 UI 仍要求用户填写 API Key、Base URL 和模型配置。
- 关闭窗口时，用户不清楚正在运行的任务会被停止。
- 缺少最小的版本更新入口、隐私说明、发行说明和反馈入口。

---

## Task 1 — 完成可信发行物（进行中）

状态：`[-] 进行中`

### 要做

- 完成 Developer ID 签名、公证、staple 和 Gatekeeper 验证，任何一步失败都不能报告发行成功。
- 使用 `notarytool`，不再使用已经停止接受上传的 `altool`。
- 用一个版本值驱动应用版本和 DMG 文件名。
- 一条明确命令从当前源码生成版本化 DMG。
- DMG 中只包含可以直接安装的 `MediaBrief.app` 和 Applications 快捷方式。

### 当前进展

2026-08-13：

- 已重写 `scripts/sign_and_package.sh`，签名、公证、staple、DMG 内容和 Gatekeeper 检查均改为失败即停止。
- 已完成真实 Developer ID 签名、secure timestamp、嵌套代码验证、Deno 运行验证和签名后应用启动验证。
- 已生成并验证签名版 `dist/MediaBrief-1.0.0-macos-arm64.dmg`。
- 当前 DMG 尚未完成 Apple 公证，仍不是正式发行物。
- 单一版本来源尚未完成。

### 完成标准

- 正式 DMG 完成签名、公证和 staple。
- 从 DMG 拖入 Applications 后可以正常打开。
- 应用版本与 DMG 文件名一致。

---

## Task 2 — 让双击启动可靠

状态：`[ ] 未开始`

签名工作由用户继续推进时，代码侧可以直接开始本任务。

### 要做

- 自动选择可用的本地端口，不依赖 8000 空闲。
- 避免同时启动多个后端实例争抢数据和端口。
- 主窗口尽早出现；重型模型和依赖加载不能让窗口长时间无响应。
- 启动失败时显示重试、复制诊断、打开日志目录和退出。
- 所有启动错误统一写入 Application Support，不写 `.app` bundle。
- 有任务运行时关闭窗口，要明确提示任务将被停止；确认后清理子进程。

### 完成标准

- 8000 被占用时仍能启动。
- 后端启动失败时不会永久转圈。
- 连续双击不会产生两个同时工作的 MediaBrief 实例。
- 用户可以自行找到日志并重试。
- 退出后没有残留 FFmpeg、yt-dlp 或 uvicorn 进程。

---

## Task 3 — 装完即可完成第一次任务

状态：`[ ] 未开始`

前置：Task 2 完成。

### 要做

- 移除首次使用中的 API Key、Base URL、获取模型和模型供应商配置。
- 发行版直接使用内置 API 配置；不增加登录、激活、兑换或授权步骤。
- 首屏直接提供粘贴链接和拖入本地文件。
- 任务进度只显示用户能理解的阶段：读取视频、查找字幕、下载音频、转录、生成摘要。
- 成功后默认显示摘要，同时可以查看全文、复制和导出。
- 保留内置 base 模型作为本地转录兜底；首次启动不静默下载 `large-v3-turbo`。
- 只有用户提交确实需要本地转录的任务时，才说明推荐模型的大小并允许下载、取消或继续使用 base。
- 失败时区分链接不可用、本地组件问题、模型未准备和 AI 请求失败，并给出下一步。
- 重启后恢复历史记录和设置。

### 完成标准

- 新用户不进入设置就能提交任务。
- 带字幕链接无需下载大模型即可完成。
- 本地文件可以使用内置 base 模型完成。
- AI 请求失败时保留已完成的下载和转录，允许重试摘要。
- 四种 UI 语言没有新增硬编码文案。

---

## Task 4 — 发布与真实用户验收

状态：`[ ] 未开始`

前置：Task 1–3 完成。

### 要做

- 提供一个简单下载页，包含最新版本、系统要求、DMG、SHA-256、发行说明和已知限制。
- 应用内显示当前版本，并提供“检查更新”；第一版只打开新版 DMG 下载页，不做静默更新。
- 提供最小隐私说明：哪些内容在本地处理、哪些文本会直接发送给上游 AI、Cookie 不会发送、日志和数据如何删除。
- 提供打开日志目录、复制诊断和反馈入口；诊断不自动上传。
- 在没有源码、Python、Node、Homebrew 和开发者证书的 Apple Silicon Mac 上走完整安装流程。
- 测试公开 YouTube/Bilibili 链接、本地文件、无字幕任务、无网络、端口冲突、运行中退出、历史恢复和覆盖安装升级。
- 找至少 5 位非技术用户，只给 DMG 和一句产品说明，记录是否成功以及卡住的位置。

### 完成标准

- 真实下载后的 DMG 能通过 Gatekeeper 并完成第一次任务。
- 用户遇到失败时知道下一步，不需要开发者远程修环境。
- 覆盖安装新版本后历史和设置仍在。
- 下载页、隐私说明、反馈入口和发行说明可用。

## 执行顺序

```text
Task 1 可信发行物（签名/公证正在推进）
Task 2 双击启动可靠性（代码侧下一个任务）
  → Task 3 装完即可完成第一次任务
  → Task 4 干净 Mac 与真实用户 Beta
```

Task 1 的签名工作与 Task 2 可以并行；Task 3 不依赖任何账号、授权或 hosted 服务。

## 执行纪律

- 开始前阅读根目录 `AGENTS.md` 和本文件，并检查工作区已有改动。
- 一次只处理当前任务，不顺手建设未来商业化能力。
- 不提交真实 API Key、Apple 凭据、Cookie 或用户内容。
- 签名、公证、staple 和 Gatekeeper 验证必须失败关闭。
- 未经用户明确要求，不提交、不推送、不上传发行物。
- 完成任务后，只记录实际改动、验证结果和仍存在的问题。

## 下一项代码任务

> 在保留当前签名工作和其他用户改动的前提下，只执行 **Task 2 — 让双击启动可靠**。不要建设双发行版、Hosted AI、账号、激活、兑换、额度、限流或支付能力。
