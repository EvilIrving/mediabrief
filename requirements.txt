fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
yt-dlp>=2024.12.13
# ASR：Apple Silicon 专用。mlx-whisper 吃 GPU + 统一内存，较 faster-whisper(CTranslate2
# 仅 CPU) 在 M 系芯片上提速约 8–10×。pin 版本避免接口/打包漂移（拉入 mlx、mlx-metal、
# numpy、huggingface_hub）。仅 macOS/arm64；发行产品不支持 Linux / 容器部署。
mlx-whisper==0.4.3
# Silero 前置 VAD（转录前切除静音/抗幻觉）用 onnxruntime 跑 vendored 的
# silero_vad_v6.onnx（见 backend/assets/）；不引入 silero-vad 包以避免其 torch 硬依赖。
# 原为 faster-whisper 的传递依赖，现声明为直接依赖。
onnxruntime>=1.16.1
openai>=1.51.0
pydantic>=2.7.0
aiofiles>=24.1.0
trafilatura>=1.12.0
python-docx>=1.1.0
fpdf2>=2.7.0
markdown>=3.6
beautifulsoup4>=4.12.0
reportlab>=4.0.0
pywebview>=6.0
pyinstaller>=6.0
httpx[socks]>=0.27.0
