# MediaBrief 前端性能优化

## 目标

- 长 transcript / summary 不在切换记录时一次性解析成巨量 HTML/DOM。
- 历史、任务队列、RSS 订阅、RSS 条目、下载相关列表在数据量达到 1000 级别时仍可操作。
- 页面切换后释放不再使用的长文本、列表和媒体缓存。
- 所有用户可见文案继续走四语言 i18n。

## 工作项

- [x] 审计长文本加载、Markdown 渲染和列表数据路径。
- [x] 共享 Markdown 组件支持按 block 渐进渲染。
- [x] 历史页只保留当前任务 transcript，离开页面时释放。
- [x] 转录结果页改用原始 Markdown + 渐进渲染，避免预先生成完整 HTML。
- [x] 历史记录列表增加分页/渐进加载，避免一次加载 1000 条。
- [x] 历史记录列表用 memo 化派生数据、渐进 DOM 和批量选择，避免重复创建整棵列表。
- [x] 任务队列限制已完成任务数量并支持渐进显示，避免队列列表无限增长。
- [x] RSS 订阅列表增加分页/渐进加载；详情只加载当前订阅的条目。
- [x] RSS 条目列表增加渐进显示，避免一个订阅的 1000 条条目一次渲染。
- [x] 下载格式、下载历史及其他列表路径做同类检查。
- [x] 限制 TTS Blob/对象 URL 缓存，切换任务时释放旧缓存。
- [x] 为长文分块补充测试；历史/RSS 组件测试仍待补充。
- [x] 前端构建通过；完整测试被当前 jsdom 的 `localStorage.clear is not a function` 环境错误阻断。

## 约束

- 不新增重量级虚拟列表依赖；优先使用现有 React 和浏览器能力。
- 不把完整 transcript 放回历史列表接口。
- 不改变后端数据语义或删除策略，除非前端分页确实需要接口参数。
- 任务中断后可从本文件继续。
